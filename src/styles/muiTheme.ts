import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#365c74',
    },
  },
  shape: {
    borderRadius: 10,
  },
});

export default theme;
